---
name: upskilling-angular
description: Use when the user asks to add, revise, retrieve, or practice Angular learning notes in the way-to-architect repo, including standalone components, templates, signals, dependency injection, routing, forms, RxJS, testing, Angular TypeScript, frontend architecture, or Angular exercises.
---

# Upskilling Angular

Repo: `/Users/sujeithgopinath/Personal/way-to-architect/`

Primary folders:

- Curated notes: `frontend/angular/`
- Exercises: `exercises/angular/`
- TypeScript notes: `languages/typescript/`
- Frontend platform links: `frontend/browser/`, `frontend/performance/`

Workflow:

1. Search existing Angular, TypeScript, and frontend notes with `rg`.
2. Add Angular concepts and patterns under `frontend/angular/`.
3. Add practice tasks under `exercises/angular/`.
4. Put pure TypeScript material under `languages/typescript/`.
5. Cross-link browser, storage, and performance material when Angular-specific
   behavior depends on platform behavior.
6. Verify modern Angular behavior with `https://angular.dev/` before writing
   durable guidance.
7. Run `git diff --check` after edits.
