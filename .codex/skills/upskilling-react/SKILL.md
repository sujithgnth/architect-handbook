---
name: upskilling-react
description: Use when the user asks to add, revise, retrieve, or practice React learning notes in the way-to-architect repo, including components, hooks, state, effects, forms, routing, React TypeScript, testing, performance, Redux, or React exercises.
---

# Upskilling React

Repo: `/Users/sujeithgopinath/Personal/way-to-architect/`

Primary folders:

- Curated notes: `frontend/react/`
- Exercises: `exercises/react/`
- Performance notes: `frontend/performance/`
- Redux notes: `redux/`
- References: `frontend/react/references/`

Workflow:

1. Search `frontend/react/`, `frontend/react/references/`, `redux/`, and
   `exercises/react/` with `rg`.
2. Keep copied repos and third-party material under `frontend/react/references/`.
3. Extract short, curated notes into `frontend/react/`.
4. Add practice tasks under `exercises/react/`.
5. Verify modern React behavior with `https://react.dev/` before writing durable
   guidance.
6. Run `git diff --check` after edits.
