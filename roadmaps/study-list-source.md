# Study List Source

Legacy source material imported from:

<https://github.com/sujithgnth/study-list-learn-read-tech-guidlines>

The original nested `.git` directory was moved outside this repo to:

`/Users/sujeithgopinath/Personal/study-list-learn-read-tech-guidlines.git-backup`

The raw working copy may also exist locally at:

`/Users/sujeithgopinath/Personal/way-to-architect/study-list-learn-read-tech-guidlines`

That raw folder is ignored by Git because it contains bulky PDFs, screenshots,
images, and a nested Git repository.

Curated Markdown notes were moved into handbook folders such as
`architectures/`, `coding-practices/`, `frontend/`, `interviews/`, and
`roadmaps/`.

Bulky source snapshots, PDFs, and screenshots are kept locally next to their
closest topic under ignored `resources/` or raw-source folders. Prefer the
existing curated top-level folders for durable notes.

## Reconciliation

The raw source had 354 Markdown files. Of those, 352 were exact matches for
files already present in the handbook or in the ignored raw source snapshot.

The remaining useful source note was extracted to:

- `../interviews/interview-guides.md`

The remaining microservices source file is already represented by:

- `../architectures/microservices/testdrivenio-notes/README.md`

The existing microservices copy keeps corrected local image paths, so it should
remain the canonical handbook reference.
