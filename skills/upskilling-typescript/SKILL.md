---
name: upskilling-typescript
description: Use when the user asks to add, revise, retrieve, or practice TypeScript learning notes in the way-to-architect repo, including types, generics, narrowing, utility types, public interfaces, React typing, Angular typing, JavaScript interop, or TypeScript exercises.
---

# Upskilling TypeScript

Repo: `/Users/sujeithgopinath/Personal/way-to-architect/`

Primary folders:

- Notes: `languages/typescript/`
- JavaScript background: `languages/javascript/`
- React typing patterns: `frontend/react/`
- Angular typing patterns: `frontend/angular/`
- Exercises: `exercises/javascript/`, `exercises/react/`,
  `exercises/angular/`
- React TypeScript reference:
  `frontend/react/references/react-typescript-cheatsheet/`

Workflow:

1. Search existing notes with `rg`.
2. Add language-level material under `languages/typescript/`.
3. Add framework-specific typing notes under the matching framework folder.
4. Prefer realistic application examples over abstract type puzzles.
5. Verify version-sensitive behavior with official TypeScript docs before
   writing durable guidance.
6. Run `git diff --check` after edits.
